{
	"version": "0.1",
	"id": "cmd_000073",
	"title": "Combate: puno con F + espada clasica en el StarterPack",
	"created_by": "notion-agent",
	"created_at": "2026-08-26T18:25:00Z",
	"request": "El usuario pide armas o una forma de eliminar a los ladrones. Dos vias: (1) PUNO con la tecla F: CombatClient (LocalScript en DeliveryClient) avisa al servidor y RigCombatService (Script en ServerScriptService) valida distancia (<=7 studs), aplica 34 de dano (3 golpes a un ladron de 100 HP) y un empujon; al morir, el propio RigThief se desvanece y desaparece. Crea el RemoteEvent Punch dentro de ReplicatedStorage.DeliveryRemotes si no existe. (2) ESPADA: inserta la Linked Sword clasica de Roblox (asset 125013769) en StarterPack con allow_scripts=true, lo que por diseño del bridge exige aprobacion humana en el plugin. Nota: la espada tambien puede danar a otros jugadores (PvP).",
	"options": { "atomic": false, "dry_run": false, "require_approval": false, "create_waypoint": true },
	"operations": [
		{ "id": "op_1", "op": "ensure_instance", "path": "ServerScriptService.RigCombatService", "class": "Script" },
		{ "id": "op_2", "op": "set_script_source", "path": "ServerScriptService.RigCombatService", "source": "--!nonstrict\n--============================================================\n-- 👊 RIG COMBAT · golpea a los ladrones con F (o con la espada\n-- del StarterPack). El servidor valida distancia y aplica daño.\n-- Al llegar a 0 HP, el propio RigThief se desvanece solo.\n--============================================================\nlocal Players = game:GetService(\"Players\")\nlocal ReplicatedStorage = game:GetService(\"ReplicatedStorage\")\n\nlocal PUNCH_RANGE = 7      -- studs máximos para alcanzar al ladrón\nlocal PUNCH_DAMAGE = 34    -- 3 golpes a un ladrón de 100 HP\nlocal PUNCH_COOLDOWN = 0.6 -- segundos entre golpes por jugador\n\n-- RemoteEvent propio (dentro de DeliveryRemotes si existe)\nlocal remotes = ReplicatedStorage:FindFirstChild(\"DeliveryRemotes\")\nif not remotes then\n\tremotes = Instance.new(\"Folder\")\n\tremotes.Name = \"DeliveryRemotes\"\n\tremotes.Parent = ReplicatedStorage\nend\nlocal punch = remotes:FindFirstChild(\"Punch\")\nif not punch then\n\tpunch = Instance.new(\"RemoteEvent\")\n\tpunch.Name = \"Punch\"\n\tpunch.Parent = remotes\nend\n\n-- ladrones vivos: clones del spawner + el Rig original\nlocal function thieves()\n\tlocal list = {}\n\tlocal folder = workspace:FindFirstChild(\"RigThieves\")\n\tif folder then\n\t\tfor _, m in ipairs(folder:GetChildren()) do\n\t\t\tif m:IsA(\"Model\") and m:FindFirstChildOfClass(\"Humanoid\") then\n\t\t\t\ttable.insert(list, m)\n\t\t\tend\n\t\tend\n\tend\n\tlocal rig = workspace:FindFirstChild(\"Rig\")\n\tif rig and rig:IsA(\"Model\") and rig:FindFirstChildOfClass(\"Humanoid\") then\n\t\ttable.insert(list, rig)\n\tend\n\treturn list\nend\n\nlocal lastPunch = {}\npunch.OnServerEvent:Connect(function(player)\n\tlocal now = os.clock()\n\tif lastPunch[player] and now - lastPunch[player] < PUNCH_COOLDOWN then return end\n\tlastPunch[player] = now\n\tlocal char = player.Character\n\tlocal hrp = char and char:FindFirstChild(\"HumanoidRootPart\")\n\tif not hrp then return end\n\t-- ladrón vivo más cercano\n\tlocal best, bestDist, bestHum\n\tfor _, m in ipairs(thieves()) do\n\t\tlocal hum = m:FindFirstChildOfClass(\"Humanoid\")\n\t\tlocal r = m:FindFirstChild(\"HumanoidRootPart\")\n\t\tif hum and r and hum.Health > 0 then\n\t\t\tlocal d = (r.Position - hrp.Position).Magnitude\n\t\t\tif d <= PUNCH_RANGE and (not bestDist or d < bestDist) then\n\t\t\t\tbest, bestDist, bestHum = m, d, hum\n\t\t\tend\n\t\tend\n\tend\n\tif not best then return end\n\t-- daño + empujón\n\tbestHum:TakeDamage(PUNCH_DAMAGE)\n\tlocal r = best:FindFirstChild(\"HumanoidRootPart\")\n\tif r then\n\t\tlocal dir = r.Position - hrp.Position\n\t\tdir = Vector3.new(dir.X, 0, dir.Z)\n\t\tif dir.Magnitude > 0.01 then\n\t\t\tr:ApplyImpulse(dir.Unit * r.AssemblyMass * 30 + Vector3.new(0, r.AssemblyMass * 12, 0))\n\t\tend\n\tend\n\tprint(\"[Combat] \" .. player.Name .. \" golpeó a \" .. best.Name .. \" (queda \" .. math.max(0, math.floor(bestHum.Health)) .. \" HP)\")\nend)\n\nprint(\"[RigCombat] listo — los jugadores golpean con F\")\n" },
		{ "id": "op_3", "op": "ensure_instance", "path": "StarterPlayer.StarterPlayerScripts.DeliveryClient.CombatClient", "class": "LocalScript", "create_parents": true },
		{ "id": "op_4", "op": "set_script_source", "path": "StarterPlayer.StarterPlayerScripts.DeliveryClient.CombatClient", "source": "--!nonstrict\n--============================================================\n-- 👊 COMBAT CLIENT · pulsa F para golpear al ladrón cercano\n-- (el servidor valida distancia, daño y enfriamiento)\n--============================================================\nlocal UserInputService = game:GetService(\"UserInputService\")\nlocal ReplicatedStorage = game:GetService(\"ReplicatedStorage\")\n\nlocal remotes = ReplicatedStorage:WaitForChild(\"DeliveryRemotes\")\nlocal punch = remotes:WaitForChild(\"Punch\")\n\nUserInputService.InputBegan:Connect(function(input, gameProcessed)\n\tif gameProcessed then return end\n\tif input.KeyCode == Enum.KeyCode.F then\n\t\tpunch:FireServer()\n\tend\nend)\n\nprint(\"[Delivery60] Combate listo: pulsa F para golpear ladrones\")\n" },
		{ "id": "op_5", "op": "insert_asset", "asset_id": 125013769, "path": "StarterPack", "name": "Espada", "allow_scripts": true }
	]
}
